---
description: Run the TDD-as-design Red→Green→Refactor cycle from an approved LLD — generates tests first, then implementation
argument-hint: "[LLD path or issue number and component name]"
---

Invoke the `tdd` skill with $ARGUMENTS.

Do not generate any implementation code before the test suite is reviewed and RED is confirmed.
