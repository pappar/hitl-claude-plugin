---
description: Run semgrep, manifest drift, and Mermaid convention checks against the codebase
argument-hint: "[--only semgrep|manifest|mermaid]"
---

Invoke the `check-conventions` skill with $ARGUMENTS.
