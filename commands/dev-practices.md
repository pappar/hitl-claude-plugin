---
description: Load the HITL development practices workflow for a change
argument-hint: "[change description or issue number]"
---

Invoke the `dev-practices` skill with $ARGUMENTS.

Do not start implementing until the change tier is identified and the source artifact chain is confirmed (issue → HLD/LLD → manifest domain).
