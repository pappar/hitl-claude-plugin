---
description: Review and apply infrastructure changes — run plan, present changes for operator approval, apply, and verify state
argument-hint: "[change ID or IaC directory path]"
---

Invoke the `ops-apply-iac` skill with $ARGUMENTS.

Do not apply until the plan has been reviewed and explicitly approved by the operator. Destructive changes require a second confirmation.
