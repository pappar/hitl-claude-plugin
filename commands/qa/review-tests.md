---
description: QA review — verify test coverage against acceptance criteria, LLD edge cases, and incident registry regressions before implementation starts
argument-hint: "[feature name, PR link, or LLD path]"
---

Invoke the `qa-review-tests` skill with $ARGUMENTS.

Do not approve until all acceptance criteria are covered, all LLD error modes are tested, incident regressions from the registry are present, and the test registry is updated.
