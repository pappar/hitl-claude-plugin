---
description: Generate a downstream impact brief and rollout plan for a change ready for PR
argument-hint: "[PR number, branch name, or change description]"
---

Invoke the `impact-brief` skill with $ARGUMENTS.

Do not create the PR until the impact brief is reviewed and the HITL context file is updated with evidence.
